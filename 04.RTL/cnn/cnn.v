/*
 * Module: cnn
 * Processes the 32x32x1 vibration spectrogram (conv + ReLU + pooling +
 * classification) on an independent path from the numeric classifier.
 * Status: not implemented yet.
 */
module cnn (
    input  wire clk,
    input  wire rst_n
);

endmodule
