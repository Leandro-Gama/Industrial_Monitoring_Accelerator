/*
 * Module: ml_classifier
 * Classifies motor state (normal, unbalance, misalignment, bearing wear)
 * from FFT/MDC, LMS and matrix-inversion features.
 * Status: not implemented yet.
 */
module ml_classifier (
    input  wire clk,
    input  wire rst_n
);

endmodule
