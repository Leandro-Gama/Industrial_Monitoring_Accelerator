/*
 * Module: mdc
 * Greatest common divisor of the detected FFT peak bins, used to estimate
 * the fundamental bin k0 for the SMMA fundamental-frequency path.
 * Status: not implemented yet.
 */
module mdc (
    input  wire clk,
    input  wire rst_n
);

endmodule
