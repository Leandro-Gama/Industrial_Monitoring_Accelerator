/*
 * Module: fft
 * 64-point FFT of the vibration signal, feeding the peak detector and the
 * spectrogram path for the CNN.
 * Status: not implemented yet.
 */
module fft (
    input  wire clk,
    input  wire rst_n
);

endmodule
